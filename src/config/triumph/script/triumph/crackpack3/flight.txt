setTitle("Flight")

setIcon(<minecraft:feather>)

setDescription("Some types of flight in the pack, such as the flight ability on various Metamorph morphs, are locked behind The End. Travel through an End Gateway towards the End Cities to unlock. Watch out for that pesky dragon!")

addParent("triumph:crackpack3/root")

hideLines()

setShowToast(false)

setAnnounceToChat(false)

addCriteria("location", "minecraft:location")
setPos(112,-108)
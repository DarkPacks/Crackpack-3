setTitle("Dimensions")

setIcon(<rats:ratlantis_portal>)

setDescription("Some dimensions in the pack, such as the Nether and the End, can only be accessed using the Stacked Dimensions mod.")

addParent("triumph:crackpack3/root")

hideLines()

setShowToast(false)

setAnnounceToChat(false)

addCriteria("location", "minecraft:location")
setPos(174,-108)